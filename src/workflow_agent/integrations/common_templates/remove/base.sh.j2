#!/usr/bin/env bash
set -e

{% from "macros/common.sh.j2" import log_message %}
{{ log_message("INFO", "Starting removal for " + template_data.get("name", "unknown")) }}

{% for cmd in template_data.get('remove', {}).get('commands', []) %}
{{ cmd }}
{% endfor %}

echo "Removal complete."